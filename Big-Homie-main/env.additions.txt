# ============================================================
# BIG HOMIE — Revenue Verticals .env additions
# Append to your existing .env:  cat env.additions.txt >> .env
# ============================================================

# ── opencode (default coding engine) ──────────────────────
OPENCODE_URL=http://localhost:4111/v1
OPENCODE_ENABLED=true
DEFAULT_CODING_MODEL=opencode

# ── Vocal synthesis ────────────────────────────────────────
ELEVENLABS_API_KEY=          # elevenlabs.io free tier
BARK_MODEL_PATH=             # optional local Bark TTS path
BARK_ENABLED=false

# ── Media generation (verify existing keys) ───────────────
ENABLE_MEDIA_GENERATION=true
MINIMAX_API_KEY=
MINIMAX_GROUP_ID=
MINIMAX_ENABLED=true
COMFYUI_ENABLED=true
COMFYUI_BASE_URL=http://localhost:8188
GOOGLE_LYRIA_API_KEY=
GOOGLE_LYRIA_ENABLED=false
MEDIA_OUTPUT_DIR=~/.big_homie/media_outputs
MAX_MEDIA_GENERATION_TIME=300
ENABLE_ASYNC_MEDIA_TASKS=true

# ── Cloudflare (deploy + R2 storage) ──────────────────────
CLOUDFLARE_API_TOKEN=
CF_ACCOUNT_ID=
CF_R2_BUCKET=big-homie-media
CF_R2_ACCESS_KEY=
CF_R2_SECRET_KEY=
CF_PAGES_PROJECT=big-homie-sites

# ── Content delivery ───────────────────────────────────────
RESEND_API_KEY=              # resend.com — free 3k/mo
PEXELS_API_KEY=              # pexels.com/api — free 200 req/hr

# ── Search (content research) ─────────────────────────────
BRAVE_API_KEY=               # api.search.brave.com — free 2k/mo

# ── GSD vertical flags ────────────────────────────────────
ENABLE_CONTENT_FACTORY=true
ENABLE_RAP_VIDEO_ENGINE=true
ENABLE_SITE_BUILDER=true

# ── Revenue targets ────────────────────────────────────────
CONTENT_PACK_PRICE_USD=29.00
RAP_VIDEO_PRICE_USD=49.00
SITE_BUILD_PRICE_USD=29.00
SITE_MONTHLY_PRICE_USD=99.00

# ── Stripe price IDs (create at dashboard.stripe.com) ─────
STRIPE_CONTENT_PACK_PRICE_ID=
STRIPE_RAP_VIDEO_PRICE_ID=
STRIPE_SITE_BUILD_PRICE_ID=
